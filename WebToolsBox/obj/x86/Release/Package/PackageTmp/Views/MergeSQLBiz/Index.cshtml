@model WebToolsBox.QueryEntity
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<div class="container-fluid">

</div>
<hr />

<div class="container-fluid">
    <div class="form-group">
            @using(Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true)

                <div class="col-sm-10">
                    <label class="sr-only" for="code_input">请输入查询交易代码（'|'分割）</label>
                    <input type="text" name="searchText" class="form-control" id="code_input" placeholder="请输入查询交易代码（'|'分割）">
                </div>
                <div class="col-sm-2">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon glyphicon-search" aria-hidden="true"></span>&nbsp;&nbsp;生成
                    </button>
                </div>
            }
    </div>
</div>

<hr />

<div class="container-fluid">
    <ul class="nav nav-tabs">
        <li class="active"><a href="#home" data-toggle="tab" aria-expanded="true">嵌入语句</a></li>
        <li class=""><a href="#profile" data-toggle="tab" aria-expanded="false">嵌入日志</a></li>
    </ul>
    <div id="InsetClause" class="tab-content">
        @if(Model != null)
        {
            <div class="tab-pane fade active in" id="home">
            <div>
                <br />
                <br />

                @if (Model.ErrorCodeQuery.Count > 0)
                {
                    <br /><p>--Error_Code 表操作</p><br />
                    
                    foreach (string str in Model.ErrorCodeQuery)
                    {
                    <p>@str</p>
                    <br />}
                }
                
                @if (Model.ErrorGroupQuery.Count > 0)
                {
                    <br /><p>--Error_Group 表操作</p><br />
                    
                    foreach (string str in Model.ErrorGroupQuery)
                    {
                    <p>@str</p>
                    <br />}
                }
                
                @if (Model.ErrorGroupMappingQuery.Count > 0)
                {
                    <br /><p>--Error_Group 表操作</p><br />
                    
                    foreach (string str in Model.ErrorGroupMappingQuery)
                    {
                    <p>@str</p>
                    <br />}
                }
                
                @if (Model.DynamicDetailQuery.Count() > 0)
                {
                    <br /><p>--DYNAMIC_DETAIL表操作--</p><br />
                    foreach (string str in Model.DynamicDetailQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.DynamicItemQuery.Count() > 0)
                {
                    <br /><p>--DYNAMIC_ITEM 表操作--</p><br />
                    foreach (string str in Model.DynamicItemQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.DynamicValidQuery.Count() > 0)
                {
                    <br /><p>--DYNAMIC_VALID 表操作--</p><br />
                    foreach (string str in Model.DynamicValidQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.FieldMessageQuery.Count() > 0)
                {
                    <br /><p>--FIELD_MESSAGE 表操作</p><br />
                    
                    foreach (string str in Model.FieldMessageQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.FunctionInfoQuery.Count() > 0)
                {
                    <br /><p>--FUNCTION_INFO 表操作</p><br />
                    
                    foreach (string str in Model.FunctionInfoQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.OperationInfoQuery.Count() > 0)
                {
                    <br /><p>--OPERATION_INFO 表操作</p><br />
                    
                    foreach (string str in Model.OperationInfoQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.PrintModuleGroupMemberQuery.Count() > 0)
                {
                    <br /><p>--PRINT_MODULE_GROUP_MEMBER 表操作</p><br />
                    
                    foreach (string str in Model.PrintModuleGroupMemberQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.PrintModuleGroupQuery.Count() > 0)
                {
                    <br /><p>--PRINT_MODULE_GROUP 表操作</p><br />
                    
                    foreach (string str in Model.PrintModuleGroupQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TblDispContentQuery.Count() > 0)
                {
                    <br /><p>--TBL_DISP_CONTENT 表操作</p><br />
                    
                    foreach (string str in Model.TblDispContentQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TblExchangeQuery.Count() > 0)
                {
                    <br /><p>--TBL_EXCHANGE 表操作</p><br />
                    
                    foreach (string str in Model.TblExchangeQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TBLPrintModuleQuery.Count() > 0)
                {
                    <br /><p>--TBL_PRINT_MODULE 表操作</p><br />
                    
                    foreach (string str in Model.TBLPrintModuleQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TransAutoQuery.Count() > 0)
                {
                    <br /><p>--TRANS_AUTO 表操作</p><br />
                    
                    foreach (string str in Model.TransAutoQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TransDefQuerys.Count() > 0)
                {
                    <br /><p>--TRANS_DEF 表操作</p><br />
                    
                    foreach (string str in Model.TransDefQuerys)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TransSplitQuery.Count() > 0)
                {
                    <br /><p>--TRANS_SPLIT 表操作</p><br />
                    
                    foreach (string str in Model.TransSplitQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.VoidConfigQuery.Count() > 0)
                {
                    <br /><p>--VOID_CONFIG 表操作</p><br />
                    
                    foreach (string str in Model.VoidConfigQuery)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TransCommandsQuery.Count() > 0)
                {
                    <br /><p>--TRANS_COMMANDS 表操作</p><br />
                    foreach (string str in Model.TransCommandsQuery)
                    {
                        <p>@str</p>
                        <br />
                    }
                }
            </div>
        </div>
            <div class="tab-pane fade" id="profile">
            <div>
                @if (Model.ErrorCodeAddRecord.Count > 0)
                {
                    <br /><p>--Error_Code 表操作</p><br />
                    
                    foreach (string str in Model.ErrorCodeAddRecord)
                    {
                    <p>@str</p>
                    <br />}
                }
                
                @if (Model.ErrorGroupAddRecord.Count > 0)
                {
                    <br /><p>--Error_Group 表操作</p><br />
                    
                    foreach (string str in Model.ErrorGroupAddRecord)
                    {
                    <p>@str</p>
                    <br />}
                }
                
                @if (Model.ErrorGroupMappingAddRecord.Count > 0)
                {
                    <br /><p>--Error_Group 表操作</p><br />
                    
                    foreach (string str in Model.ErrorGroupMappingAddRecord)
                    {
                    <p>@str</p>
                    <br />}
                }

                @if (Model.DynamicDetailAddRecord.Count() > 0)
                {
                    <br /><p>--DYNAMIC_DETAIL 表操作</p><br />
                    
                    foreach (string str in Model.DynamicDetailAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.DynamicItemAddRecord.Count() > 0)
                {
                    <br /><p>--DYNAMIC_ITEM 表操作</p><br />
                    
                    foreach (string str in Model.DynamicItemAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.DynamicValidAddRecord.Count() > 0)
                {
                    <br /><p>--DYNAMIC_VALID 表操作</p><br />
                    
                    foreach (string str in Model.DynamicValidAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.FieldMessageAddRecord.Count() > 0)
                {
                    <br /><p>--FIELD_MESSAGE 表操作</p><br />
                    
                    foreach (string str in Model.FieldMessageAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.FunctionInfoAddRecord.Count() > 0)
                {
                    <br /><p>--FUNCTION_INFO 表操作</p><br />
                    
                    foreach (string str in Model.FunctionInfoAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.OperationInfoAddRecord.Count() > 0)
                {
                    <br /><p>--OPERATION_INFO 表操作</p><br />
                    
                    foreach (string str in Model.OperationInfoAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.PrintModuleGroupMemberAddRecord.Count() > 0)
                {
                    <br /><p>--PRINT_MODULE_GROUP_MEMBER 表操作</p><br />
                    
                    foreach (string str in Model.PrintModuleGroupMemberAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.PrintModuleGroupAddRecord.Count() > 0)
                {
                    <br /><p>--PRINT_MODULE_GROUP 表操作</p><br />
                    
                    foreach (string str in Model.PrintModuleGroupAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TblDispContentAddRecord.Count() > 0)
                {
                    <br /><p>--TBL_DISP_CONTENT 表操作</p><br />
                    
                    foreach (string str in Model.TblDispContentAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TblExchangeAddRecord.Count() > 0)
                {
                    <br /><p>--TBL_EXCHANGE 表操作</p><br />
                    foreach (string str in Model.TblExchangeAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TBLPrintModuleAddRecord.Count() > 0)
                {
                    <br /><p>--TBL_PRINT_MODULE 表操作</p><br />
                    foreach (string str in Model.TBLPrintModuleAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.TransAutoAddRecord.Count() > 0)
                {
                    <br /><p>--TRANS_AUTO 表操作</p><br />
                    foreach (string str in Model.TransAutoAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

           
                @if (Model.TransSplitAddRecord.Count() > 0)
                {
                    <br /><p>--TRANS_SPLIT 表操作</p><br />
                
                    foreach (string str in Model.TransSplitAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }

                @if (Model.VoidConfigAddRecord.Count() > 0)
                {
                    <br /><p>--VOID_CONFIG 表操作</p><br />
                    
                    foreach (string str in Model.VoidConfigAddRecord)
                    {
                    <p>@str</p>
                    <br />
               }
                }
            </div>
        </div>
        }
    </div>
</div>
